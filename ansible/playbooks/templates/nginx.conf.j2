events {}

http {
  server {
    listen 80;
    server_name {{ ansible_hostname }};
    server_tokens off;

    location / {
      root {{ webShare }};
      autoindex on;
      autoindex_exact_size off;
      autoindex_format html;
      autoindex_localtime on;
    }
  }
}